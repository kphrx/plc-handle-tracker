#extend("templates/search"):
  #export("status"):
    <dl>
      <dt>Known handles count</dt>
      <dd>#(count)</dd>
    </dl>
    #if(message): <p class=message>#(message)</p> #endif
  #endexport

  #export("search"):
    <form action=/handle method=get>
      <label>
        Handle:
        <span data-prefix=@><input type=search name=name placeholder=example.bsky.social></span>
      </label>
      <button>Search</button>
    </form>
  #endexport
#endextend
