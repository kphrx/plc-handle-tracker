#extend("templates/search"):
  #export("status"):
    <dl>
      <dt>Known DIDs count</dt>
      <dd>#(count)</dd>
    </dl>
    #if(message): <p class=message>#(message)</p> #endif
  #endexport

  #export("search"):
    <form action=/did method=get>
      <label>DID: <input type=search name=name placeholder=did:plc:example></label>
      <button>Search</button>
    </form>
  #endexport
#endextend
